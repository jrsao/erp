common:
    resource: "@CommonBundle/Controller/"
    type:     annotation
    prefix:   /

coordinate:
    resource: "@CoordinateBundle/Controller/"
    type:     annotation
    prefix:   /

person:
    resource: "@PersonBundle/Controller/"
    type:     annotation
    prefix:   /

site:
    resource: "@SiteBundle/Controller/"
    type:     annotation
    prefix:   /

common_bundle_directive_property_template:
    path: /directivePropertyTemplate
    defaults:
        _controller: FrameworkBundle:Template:template
        template:    'CommonBundle:Macro:template.html.twig'